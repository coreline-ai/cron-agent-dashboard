# implement_20260522_212332.md

작성 일시: `2026-05-22 21:23:32 KST`

이 문서는 TODO.md의 잔여 후속 항목 중 한 사이클로 닫을 수 있는 5트랙을 묶는다.

## 개발 목적

직전 사이클(`implement_20260522_183344.md`)이 4개 큰 트랙을 마무리한 뒤 남은 후속 항목들을 운영성·완결성 위주로 정리한다. 큰 명세 결정이 필요한 항목(S3 storage / 바이러스 스캔 / 대량 데이터 벤치마크 / workspace 차원 chain dashboard)은 본 plan 범위 밖.

## 개발 범위

- **Track A — ROADMAP 본문 재분류**: `docs/ROADMAP.md`의 unchecked 항목을 done/deferred/superseded로 명시. v0.1 출시 후 작업은 TODO/CHANGELOG에서 추적한다는 상단 callout과 정합.
- **Track B — webhook 후속**: `issue.cancelled` 이벤트 type 추가 + workspace webhook에 `mask_pii` 옵션. body/title 텍스트를 dispatch 직전 마스킹.
- **Track C — attachment 후속**: 업로드 시 매직바이트 기반 MIME deep-sniff (claimed Content-Type과 mismatch면 거부 또는 sniff 결과로 교체) + 다운로드 endpoint에서 audit log 이벤트 기록.
- **Track D — Auto-chain chain cancel**: `POST /api/runs/chain/{chain_id}/cancel` 라우트 + ChainSummaryPanel에 cancel 버튼.
- **Track E — log retention UX**: Settings UI에 `auto_cleanup_log_days` 표시 + 최근 cleanup 결과(시각/회수). 기존 maintenance 결과를 별도 system_state 테이블에 기록.

`knownNoiseLines` 모니터링은 코드 변경이 필요하지 않은 운영 항목이라 Track A에 합쳐 처리(현재 패턴 1개만 등록되어 있음을 ROADMAP에 명시).

## 제외 범위

- 대량 데이터 성능 검증 (이슈 1,000+ benchmark) — 별도 plan.
- attachment 후속의 외부 storage(S3/MinIO) / 바이러스 스캔 / comment 첨부 / image inline preview — 각 별도 plan.
- chain depth/cost guard 임계치 시각화 / workspace 차원 chain dashboard — 별도 plan.
- webhook 다회 retry / exponential backoff / dead-letter — 별도 plan.

## 참조 문서

- [TODO.md](../TODO.md) — Backlog Phase 2+ 잔여 후속 7항목.
- [dev-plan/implement_20260521_224221.md](./implement_20260521_224221.md) — webhook 베이스.
- [dev-plan/implement_20260522_174204.md](./implement_20260522_174204.md) — attachment 베이스.
- [dev-plan/implement_20260522_182604.md](./implement_20260522_182604.md) — ChainSummaryPanel 베이스.

## 공통 진행 규칙

- TDD-first.
- 트랙별 1 commit. trailer `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`.
- Phase 완료 시 본 plan + TODO.md 체크박스 갱신 후 main push.
- 본 plan에 없는 범위 확장 금지.

## Phase 상태 요약

- [x] Track A. ROADMAP 본문 재분류 + knownNoiseLines 모니터링 메모
- [x] Track B. webhook issue.cancelled + payload masking
- [ ] Track C. attachment MIME deep-sniff + 다운로드 audit log
- [ ] Track D. Auto-chain chain 단위 cancel
- [ ] Track E. log retention UX (Settings + last cleanup)

---

## Track A. ROADMAP 본문 재분류

### 목표

`docs/ROADMAP.md`에서 unchecked로 남은 항목을 done(이미 구현됨) / deferred(별도 plan으로 이관) / superseded(설계 변경으로 무효) 중 하나로 명시. 운영자가 ROADMAP만 보고 "여기 적힌 게 다 안된 거야?"라는 오해를 하지 않도록.

### 구현 태스크

- [x] Phase A.1 — ROADMAP 본문 unchecked 50+ 줄 점검.
- [x] Phase A.2 — done은 [x] + 구현 위치 라벨. superseded는 callout 참조 라벨. 성능 검증 3건은 `→ deferred` 표기 유지.
- [x] Phase A.3 — `knownNoiseLines 모니터링` 메모를 ROADMAP "로그 관리" 절에 한 줄 추가.
- [x] Phase A.4 — TODO.md `knownNoiseLines` / ROADMAP 재분류 항목 [x] 갱신.

### 자체 테스트

- [x] `grep "^- \[ \]" docs/ROADMAP.md` 결과는 성능 검증 deferred 3건만 (의도적).
- [x] 갱신된 항목 모두 구현 위치 라벨 부착.

### 완료 조건

- [x] 구현 완료 / commit + main push

---

## Track B. webhook issue.cancelled + payload masking

### 목표

`issue.cancelled` lifecycle 이벤트를 webhook이 받을 수 있게 한다. 별도로, workspace webhook subscription에 `mask_pii` 옵션을 추가해 dispatch 직전에 payload의 title/body/content 텍스트를 mask(email/phone)한다.

### 구현 태스크

- [x] Phase B.1 — TDD: `TestWebhookDeliveryEnqueuedOnIssueCancelled` + `TestWebhookMaskPIIRedactsPayloadOnEnqueue`.
- [x] Phase B.2 — `UpdateIssue`의 cancelled 분기에서 `enqueueIssueCancelledWebhookTx` 호출. workspace + issue payload (run 블록 없음).
- [x] Phase B.3 — migration `0021_webhook_mask_pii.sql` + `Webhook.MaskPII` + `UpsertWebhookInput.MaskPII *bool` + CRUD 통과.
- [x] Phase B.4 — `EnqueueWebhookDeliveries`가 per-subscription 마스킹 적용. `internal/store/pii.go` (email + phone 정규식, app/ 의존 없이).
- [x] Phase B.5 — `WorkspaceWebhookSection`에 마스킹 체크박스 + 등록된 row에 "PII 마스킹" 배지.
- [x] Phase B.6 — payload 마스킹 검증은 Phase B.1의 두 번째 테스트로 일체화 (delivery row의 payload_json 직접 검사).

### 자체 테스트

- [x] `go test -race ./internal/store/...` 통과
- [x] `go test -race ./...` 통과
- [x] `pnpm --filter web test` 통과
- [x] `pnpm --filter web build` 통과

### 완료 조건

- [x] 구현 완료 / 테스트 통과 / commit + main push

---

## Track C. attachment MIME deep-sniff + 다운로드 audit log

### 목표

업로드 시 claimed Content-Type을 신뢰하지 않고 첫 512바이트 매직바이트로 sniff. mismatch면 sniff 결과로 교체(거부는 너무 공격적). 다운로드 endpoint는 호출 시마다 `attachment_downloaded` run_event를 issue context에 남긴다.

### 구현 태스크

- [ ] Phase C.1 — TDD: PNG 매직바이트(`89 50 4E 47`)를 가진 파일을 `text/plain`으로 claimed 업로드 → 저장된 row의 content_type이 `image/png`로 교정.
- [ ] Phase C.2 — store/attachments.go의 CreateAttachment에 `DetectedContentType` 인자 또는 helper. http handler에서 `http.DetectContentType(first512Bytes)`로 sniff 후 비교.
- [ ] Phase C.3 — TDD: 다운로드 endpoint가 응답 OK + 첫 호출에서 issue.run_events에 `attachment_downloaded` row 생성.
- [ ] Phase C.4 — store/issues 또는 별도 helper에 `RecordAttachmentDownload(issueID, attachmentID, by)` 추가. download handler에서 호출.
- [ ] Phase C.5 — models.go의 RunEvent 상수에 `RunEventAttachmentDownloaded = "attachment_downloaded"` 추가.

### 자체 테스트

- [ ] `go test -race ./internal/store/...` 통과
- [ ] `go test -race ./internal/httpapi/...` 통과
- [ ] `go test -race ./...` 통과

### 완료 조건

- [ ] 구현 완료 / 테스트 통과 / commit + main push

---

## Track D. Auto-chain chain 단위 cancel

### 목표

`POST /api/runs/chain/{chain_id}/cancel`로 동일 chain_id에 속한 queued/running run을 일괄 cancel. ChainSummaryPanel에 "체인 전체 취소" 버튼 추가.

### 구현 태스크

- [ ] Phase D.1 — TDD: chain에 queued 2 + running 1 + completed 1이 있을 때, chain cancel → queued 2개 + running 1개가 cancelled, completed 1개는 terminal state 유지.
- [ ] Phase D.2 — store: `CancelRunsByChain(ctx, chainID, reason) (count int, err error)`. 이미 terminal인 run은 skip.
- [ ] Phase D.3 — http: `POST /api/runs/chain/{chain_id}/cancel` 라우트. workspace 멤버십 검증.
- [ ] Phase D.4 — UI: `ChainSummaryPanel`의 각 chain row에 "체인 취소" 버튼 (마지막 status가 queued/running일 때만). 호출 후 issue refetch.
- [ ] Phase D.5 — TDD (vitest): cancel 호출 후 row의 status pill이 cancelled로 변경되는지.

### 자체 테스트

- [ ] `go test -race ./internal/store/...` 통과
- [ ] `go test -race ./internal/httpapi/...` 통과
- [ ] `pnpm --filter web test` 통과
- [ ] `pnpm --filter web build` 통과

### 완료 조건

- [ ] 구현 완료 / 테스트 통과 / commit + main push

---

## Track E. log retention UX (Settings + last cleanup)

### 목표

운영자가 Settings에서 "로그 보존 기간 며칠"과 "지난 cleanup 결과(시각/회수)"를 한 화면에서 볼 수 있게 한다. `auto_cleanup_log_days` 자체는 CLI/env로 설정하지만 UI에 readonly 표시 + 마지막 cleanup 메타데이터.

### 구현 태스크

- [ ] Phase E.1 — TDD: `maintenance` run 후 `system_state` 또는 기존 메타 테이블에 `last_log_cleanup_at` + `last_log_cleanup_files`가 기록되는지.
- [ ] Phase E.2 — store: 가벼운 `system_state` KV (`key TEXT PRIMARY KEY, value TEXT, updated_at TEXT`) migration + helper.
- [ ] Phase E.3 — maintenance runner: log cleanup 완료 후 `SetSystemState("last_log_cleanup_at", ...)`, `SetSystemState("last_log_cleanup_files", "...")` 호출.
- [ ] Phase E.4 — `GET /api/system/info` (또는 기존 endpoint)에 `auto_cleanup_log_days`, `last_log_cleanup_at`, `last_log_cleanup_files` 포함.
- [ ] Phase E.5 — Settings UI에 "로그 보존" 카드 추가 — 보존 기간 + 마지막 cleanup 시각/회수 표시.

### 자체 테스트

- [ ] `go test -race ./internal/store/...` 통과
- [ ] `go test -race ./internal/app/...` 통과
- [ ] `pnpm --filter web build` 통과

### 완료 조건

- [ ] 구현 완료 / 테스트 통과 / commit + main push

---

## 잔여 리스크 / 후속 과제 (본 plan 범위 밖)

- 대량 데이터 성능 검증 (이슈 1,000+ benchmark) — 별도 plan.
- attachment S3/MinIO storage, 바이러스 스캔, comment 첨부, image inline preview — 각 별도 plan.
- webhook 다회 retry / exponential backoff / dead-letter — 별도 plan.
- chain depth/cost guard 임계치 시각화 + workspace 차원 chain dashboard — 별도 plan.
