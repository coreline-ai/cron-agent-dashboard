# implement_20260524_191302.md

작성 일시: `2026-05-24 19:13:02 KST`

## 개발 목적

cron-agent-dashboard 위에 **AI dev-team 워크스페이스**를 한 명령으로 띄우는 seed를 추가한다. PM·디자이너·백엔드·프론트엔드·DB·QA·DevOps 7개 역할 에이전트가 hub-PM 패턴으로 협업해 실제 웹앱 소스를 작성한다.

운영 전략 문서: 직전 대화의 "Layer 1 — seed" — 이 plan은 Layer 1만 다룬다. Layer 2(QA-PASS 자동 종결)와 Layer 3(agent.role first-class)는 본 plan 범위 밖.

## 개발 범위

- 새 store helper `SeedDevTeam(ctx, store, slug, workingDir)` — 멱등.
- 새 CLI 명령 `cron-agent-dashboard seed-dev-team --slug NAME --working-dir PATH`.
- 7개 agent 자동 생성 (PM=main, 6 worker).
- 8개 skill 자동 등록 + agent_skill 매핑.
- workspace 설정: `auto_chain_enabled=true` / `auto_chain_max_depth=8` / `per_run_worktree=true` / `working_dir=<flag>` / `auto_close_on_run_done=false`.
- 운영자용 1-page 가이드 `docs/dev-team-workflow.md`.

## 제외 범위

- QA-PASS 자동 종결 — Layer 2 (별도 plan).
- agent.role first-class 컬럼 — Layer 3 (별도 plan, 운영 후 결정).
- 자동 PR 생성 / merge — 본 plan 범위 밖 (운영자가 직접 worktree 확인).
- 새 runtime 어댑터 — codex/claude/gemini 셋만 사용.
- 신규 skill 외 schema 변경 — 0개.

## 참조 문서

- [internal/app/seed.go](../internal/app/seed.go) — 기존 `SeedExample` 패턴.
- [cmd/cron-agent-dashboard/main.go](../cmd/cron-agent-dashboard/main.go) — CLI dispatch.
- [internal/store/auto_chain.go](../internal/store/auto_chain.go) — hub re-entry 정책.
- [internal/store/skill.go](../internal/store/skill.go) — skill / agent_skill 모델.
- 이전 cycle: [implement_20260521_214433.md](./implement_20260521_214433.md) — 기존 seed + chain UI.

## 공통 진행 규칙

- TDD-first. 멱등성, 7명 생성, skill 매핑까지 검증.
- 본 plan은 1~2 commit으로 끝남. trailer `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`.
- 본 plan에 없는 범위 확장 금지 (QA 자동 종결, role 컬럼 등은 후속 plan으로).

## 7 역할 에이전트 명세

| Name | Runtime | is_main | 책임 | 핵심 skill |
|---|---|---|---|---|
| **Lead** | claude | ✓ (hub) | 작업 분해 + 하위 멘션 + 종결 verdict 감지 | result-protocol, hub-routing |
| **Designer** | gemini |  | UI/UX 스펙 markdown + 토큰 결정 | result-protocol, ux-spec |
| **Backend** | codex |  | API 엔드포인트 / 비즈니스 로직 / 빌드 PASS | result-protocol, backend-patterns |
| **Frontend** | codex |  | React/Vue 코드 + `pnpm build` PASS | result-protocol, frontend-patterns |
| **DB** | codex |  | migration / schema 변경 / idempotent 검증 | result-protocol, db-migrations |
| **QA** | claude |  | 회귀 테스트 실행 + verdict 댓글 (`## QA-PASS|FAIL`) | result-protocol, qa-verdict |
| **DevOps** | codex |  | CI / 배포 / Dockerfile / GitHub Actions | result-protocol, devops-ci |

## 8 skill 명세 (간단 요약)

| Name | activation_mode | 대상 agent | 내용 |
|---|---|---|---|
| **result-protocol** | always | 전원 | 결과 댓글 시 `## RESULT: <CODE>` 헤더 강제. QA는 `## QA-PASS|FAIL` 헤더. 빌드 결과는 `## RESULT: BUILD-PASS|FAIL`. |
| **hub-routing** | always | Lead만 | "직접 코드/디자인 작성 금지, 한 번에 한 명에게만 멘션, QA-PASS 수신 시 issue를 done으로 처리". |
| **ux-spec** | always | Designer | 토큰 + 컴포넌트 명세 markdown 출력 형식. figma URL이 있으면 인용. |
| **backend-patterns** | always | Backend | working_dir의 Go/Node/Python 컨벤션 자동 감지, 빌드 명령 실행, 결과 헤더 박기. |
| **frontend-patterns** | always | Frontend | working_dir의 React/Vue/Svelte 컨벤션, `pnpm build` 또는 `npm test` 실행. |
| **db-migrations** | always | DB | forward-only migration 작성, 멱등성 검증 명령 실행. |
| **qa-verdict** | always | QA | 회귀 시나리오 실행 (playwright/vitest/cypress 자동 감지) + `## QA-PASS` 또는 `## QA-FAIL @Agent <사유>` 출력. |
| **devops-ci** | always | DevOps | CI workflow / Dockerfile 작업 + `make check` 또는 동등 명령 검증. |

skill content는 짧게 (~30-80 lines markdown). figma-mcp 같은 외부 도구는 향후 별도 plan.

## 구현 반영 현황

- [x] `internal/app/seed_dev_team.go` — `SeedDevTeam(ctx, st, slug, workingDir)` helper 추가.
- [x] `internal/app/seed_dev_team_test.go` — 7 agent / 8 skill / 14 assignment / idempotency 테스트 추가.
- [x] `cron-agent-dashboard seed-dev-team --slug <slug> --working-dir <path>` CLI 추가.
- [x] [docs/dev-team-workflow.md](../docs/dev-team-workflow.md) 운영 가이드 추가.
- [x] README / CHANGELOG에 사용법 반영.

## 실제 실행 현황 (2026-05-24 19:26 KST)

- [x] 기본 로컬 DB(`~/.cron-agent-dashboard/data.db`)에 `seed-dev-team --slug ai-dev-team --working-dir "$(pwd)"` 실행.
- [x] `AI Dev Team` workspace 생성 확인: agents 7 / skills 8 / assignments 14.
- [x] `127.0.0.1:18080` lab 서버에서 workspace 목록 노출 확인.

## Phase 상태 요약

- [x] Phase 1 — 7 agent + 8 skill seed helper + 테스트
- [x] Phase 2 — CLI 명령 + 가이드 문서
- [x] Phase 3 — 운영자 검증 시나리오 1회 + commit + push

---

## Phase 1. `SeedDevTeam` helper + 테스트

### 목표

`SeedExample`과 동일한 멱등 패턴으로 dev-team workspace를 만든다.

### 구현 태스크

- [x] Phase 1.1 — `internal/app/seed_dev_team.go` 신설. `SeedDevTeam(ctx, st, slug, workingDir) (SeededDevTeam, error)`.
- [x] Phase 1.2 — slug 빈 값이면 `ai-dev-team` 기본값 사용. 이미 slug가 있으면 `AlreadyHad=true` 반환.
- [x] Phase 1.3 — 7 agent instructions를 const helper로 보관. agent 생성 순서: Lead(main) → 6 worker.
- [x] Phase 1.4 — 8 skill 일괄 등록 + agent_skill 매핑 (위 표 기준).
- [x] Phase 1.5 — workspace 옵션 박기: `auto_chain_enabled=true`, `auto_chain_max_depth=8`, `per_run_worktree=true`, `auto_close_on_run_done=false`, `default_timeout_seconds=900`.
- [x] Phase 1.6 — TDD: `TestSeedDevTeamCreatesSevenAgentsEightSkillsAndAssignments`, `TestSeedDevTeamIsIdempotent`, `TestSeedDevTeamRejectsNilStore`.

### 자체 테스트

- [x] `go test -race ./internal/app/...` 통과
- [x] 호출 결과 → workspace 1개 / agent 7개(main 1) / skill 8개 / agent_skill 매핑 14개

### 완료 조건

- [x] 멱등성: 두 번 호출 시 두 번째는 AlreadyHad=true / 새 agent 0 / skill assignment upsert 유지.

---

## Phase 2. CLI `seed-dev-team` + 가이드 문서

### 목표

운영자가 `cron-agent-dashboard seed-dev-team --slug myapp --working-dir ~/code/myapp` 한 줄로 끝낸다.

### 구현 태스크

- [x] Phase 2.1 — `internal/config/config.go`에 새 플래그: `--slug`, `--working-dir` 추가.
- [x] Phase 2.2 — `cmd/cron-agent-dashboard/main.go`에 `case "seed-dev-team":` 추가, working_dir 절대 경로 정규화 + git root 자동 감지.
- [x] Phase 2.3 — 성공 시 운영자 친화 출력: workspace/agent/skill/assignment 수와 working_dir 표시.
- [x] Phase 2.4 — 새 문서 `docs/dev-team-workflow.md` — 운영 시나리오 1 page (멘션 룰, `## RESULT:` 컨벤션, QA-PASS 패턴, worktree 사용 안내).
- [x] Phase 2.5 — README 빠른 시작 절에 `seed-dev-team --slug ai-dev-team --working-dir "$(pwd)"` 추가.

### 자체 테스트

- [x] `go test ./...` 통과
- [x] `go run ./cmd/cron-agent-dashboard seed-dev-team --slug devteamtest --working-dir $(pwd)` 시도 — 새 워크스페이스가 SQLite에 박힘
- [x] 같은 명령 재실행 — existing 재사용 메시지

### 완료 조건

- [x] CLI 동작 / 가이드 문서 1 page / README 한 줄 추가.

---

## Phase 3. 운영자 검증 시나리오 + commit

### 목표

실제 dev-team workspace 운영이 1 cycle 정상 작동하는지 운영자가 직접 확인할 수 있도록 한다.

### 구현 태스크

- [x] Phase 3.1 — `docs/dev-team-workflow.md`에 "첫 이슈로 검증하기" 절: "Settings 페이지에 다크모드 토글 추가" 예시 이슈의 단계별 기대 흐름 + 운영자 체크포인트 + 실패 패턴 정리.
- [x] Phase 3.2 — 본 plan 체크박스 갱신.
- [x] Phase 3.3 — 단일 commit: `feat(seed): dev-team workspace seed (7 agents · 8 skills · hub-PM)`.
- [x] Phase 3.4 — main push.

### 자체 테스트

- [x] `go test -race ./...` clean (app 26s, httpapi 29s, all green)
- [x] `pnpm --filter web build` (no UI changes; regression clean)
- [x] `make fmt-check` pass

### 완료 조건

- [x] commit hash 본 plan에 박힘 / main push 완료.

---

## 잔여 리스크 / 후속 과제 (본 plan 범위 밖)

- **QA-PASS 자동 issue done 처리** — 운영해 보고 결정. Layer 2 별도 plan.
- **agent.role 컬럼** — 운영 후 필터/대시보드 욕구 발생 시. Layer 3 별도 plan.
- **figma-mcp / playwright-mcp 같은 외부 도구 binding** — Designer/QA skill 본문에 추후 추가 가능. 본 plan은 instructions만.
- **운영자가 dev-team workspace 여러 개 운영** — 가능하나 디스크 사용량 모니터 필요. 이미 worktree GC 있음.
- **skill content 정밀화** — 첫 운영 후 1-2 iteration에서 다듬는 게 자연스러움. 초기 버전은 보수적으로.
