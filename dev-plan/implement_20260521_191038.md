# implement_20260521_191038.md

작성 일시: `2026-05-21 19:10:38 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

MVP/P0~P3가 완료된 v0.1 안정화 상태(`e619548`)에서, 사용자가 정리한 backlog의 **P1·P2 품질 개선**과 **문서 현재화**까지를 정공으로 처리한다. 큰 기능(per-run worktree, import/export, webhook, attachment)은 본 plan 범위 밖이며 별도 plan으로 분리한다.

추천 진행 순서(사용자 제시):
1. 버그성 품질 개선 — execution 필터 SQL화 + 멘션 에러 처리
2. 보안/운영 개선 — Gemini prompt argv → stdin/임시파일
3. 설정 UX 완성 — `auto_close_on_run_done` UI 노출
4. 문서 정리 — TODO / ROADMAP / CHANGELOG / API 현재화

## 개발 범위

- `internal/store/issues.go::ListIssues`의 `Execution` 필터를 SQL WHERE 절로 이동(LIMIT 전 적용).
- `internal/worker/runtime/gemini.go::BuildCommand`의 prompt argv 노출을 **stdin pipe(우선)** 또는 임시파일로 전환.
- `web/src/pages/SettingsPage.tsx`의 `WorkspaceTimeoutRow` 영역에 `auto_close_on_run_done` toggle 추가 + UpdateWorkspaceInput 호출 연결.
- `internal/store/auto_chain.go::resolveAutoChainAgent` 단계에서 "agent not found" 와 DB error를 sentinel로 구분(`ErrNotFound` vs others) + 호출자에서 별도 메시지/event.
- `PUT /api/agents/:id` 계약 — 현재 `CreateAgentInput` 통째로 받아 full-update 동작이라는 점을 docs/API.md에 명시(또는 `UpdateAgentInput` 도입하여 partial-update 정공). 작은 옵션 → 본 plan에서는 **계약 명시 + 명확한 사이드이펙트 테스트**만.
- `web/`의 dev-only Vite/esbuild advisory 대응 — `pnpm update --filter web vite esbuild --latest` 또는 patch + `pnpm audit --prod` clean 확인 후 commit.
- TODO.md / docs/ROADMAP.md / CHANGELOG.md / docs/API.md를 현 commit(`e619548`) 기준으로 현재화.

## 제외 범위

- Auto-chain 고급 UI(chain tree/sequence 편집, stage-level resume).
- per-run worktree, workspace import/export, 외부 webhook, attachment.
- Homebrew 배포, LICENSE 파일 추가, demo seed (`--seed example`), release artifact smoke 자동화, 대량 데이터 성능 검증.
- README 본문 대규모 재작성(2026-05-20 동기화로 충분). 필요한 경우 한두 문단만 보강.

## 참조 문서

- [README](../README.md)
- [TODO](../TODO.md)
- [docs/ROADMAP](../docs/ROADMAP.md)
- [docs/API](../docs/API.md)
- [CHANGELOG](../CHANGELOG.md)
- 이전 plan: [implement_20260520_230031.md](./implement_20260520_230031.md) (chain UTF-8 + noise strip)
- 직전 commits: `25c1ec2`, `e619548`

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- 각 Phase는 TDD-first: 결함/계약 명세 테스트 → 실패 확인 → 패치 → 통과.
- Phase 1~3 (P1 항목)은 각각 commit + main push로 분리한다. Phase 4~7은 묶음 가능.

## Phase 상태 요약

- [x] Phase 1 — execution 필터 SQL-level 처리
- [x] Phase 2 — Gemini prompt argv → stdin/임시파일 전환
- [x] Phase 3 — `auto_close_on_run_done` Settings UI 노출
- [x] Phase 4 — 멘션 agent lookup 에러 처리 분리
- [x] Phase 5 — `PUT /api/agents/:id` 계약 명시 + 사이드이펙트 테스트
- [x] Phase 6 — Vite/esbuild dev-only advisory 대응
- [x] Phase 7 — TODO/CHANGELOG/API 현재화 (ROADMAP은 별도 commit로 후속)

## Phase 1. execution 필터 SQL-level 처리

### 목표

- `ListIssues(f.Execution)`가 LIMIT 적용 **이전**에 SQL WHERE로 필터링되어, 오래된 failed/done 이슈가 누락되지 않게 한다.

### 구현 태스크

- [x] `internal/store/issues_test.go` 신설 — `TestListIssuesExecutionFilterIsAppliedBeforeLimit`(10 done + 1 open, limit=10, execution=["done"] → 패치 전 9건 / 후 10건) + `TestListIssuesExecutionFilterAcceptsMultipleValues`(2 done + 2 queued + 1 cancelled, execution=["done","cancelled"] → 3건).
- [x] `internal/store/issues.go`에 `issueExecutionStatusExpr` 상수 추출 — `issueSelectBase`의 `AS execution_status` 표현식을 WHERE에서도 재사용 가능하게 분리.
- [x] `ListIssues`에서 `f.Execution`을 SQL `WHERE ... IN (?,?,...)`로 이동. LIMIT 전 적용.
- [x] 메모리 후필터 블록 제거.

### 자체 테스트

- [x] `go test -race ./internal/store/...` 통과
- [x] `go test -race ./internal/httpapi/...` 통과
- [x] 재현 테스트가 패치 전 fail → 패치 후 pass

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] commit + main push

## Phase 2. Gemini prompt argv → stdin/임시파일 전환

### 목표

- prompt 본문이 process argv(`/proc/PID/cmdline`)에 더 이상 노출되지 않는다. 자식 prompt 채널을 codex/claude와 동일한 stdin pipe로 통일하거나, stdin 미지원이면 임시파일 + `--prompt-file` 형태로 우회.

### 구현 태스크

- [x] `gemini --help` 확인 — `-p, --prompt` flag가 헤드리스 모드 트리거 + argv 텍스트가 "Appended to input on stdin (if any)"임을 확인. 즉 argv `-p ""` + stdin 본문이면 stdin이 곧 prompt.
- [x] `internal/worker/runtime/gemini.go::BuildCommand`를 `args := []string{"-p", ""}` + `commandWithPrompt`로 교체. prompt 본문은 stdin pipe(`bytes.NewReader`)로 전달.
- [x] `internal/worker/runtime/adapter_test.go`에서 기존 `TestGeminiAdapterBuildCommand`를 `TestGeminiAdapterBuildCommandPassesPromptViaStdin`으로 교체 — argv에 prompt body가 새지 않고, `-p` 또는 `--prompt` flag가 있고, model flag가 보존되며, stdin이 정확히 prompt body임을 검증.
- [x] `ParseMetrics`는 변경 없이 그대로(stderr/stdout 분리는 sanitize 패치에서 처리됨).

### 자체 테스트

- [x] `go test -race ./internal/worker/runtime/...` 통과
- [x] `go test -race ./...` clean across 10 packages

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] commit + main push

## Phase 3. `auto_close_on_run_done` Settings UI 노출

### 목표

- 워크스페이스 운영자가 Settings 페이지에서 `auto_close_on_run_done`을 ON/OFF로 직접 변경할 수 있다.

### 구현 태스크

- [x] `web/src/api/queries.ts`의 `WorkspaceSummary` 타입에 `auto_close_on_run_done?: boolean` 추가.
- [x] `web/src/pages/SettingsPage.tsx::WorkspaceTimeoutRow`에 새 state(`autoCloseOnRunDone`) + checkbox 행("run 성공 시 이슈 자동 완료 처리") + PUT payload 필드 추가. checkbox는 auto-chain checkbox 위에 배치.
- [x] `tests/e2e/settings.spec.ts`에 `TC-7.7 — auto_close_on_run_done toggle persists via PUT` 추가 — 초기 server 값을 읽고 toggle 후 PUT 응답 + GET 재확인으로 `!initial` 검증.

### 자체 테스트

- [x] `pnpm --filter web build` 통과 (vite 541 modules)
- [x] `make prepare-static` + `go build` 성공
- [x] `pnpm exec playwright test settings.spec.ts -g "TC-7.7"` 1 passed (535ms)

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] commit + main push

## Phase 4. 멘션 agent lookup 에러 처리 분리

### 목표

- auto-chain dispatch 중 `@AgentName` 해석 실패에서 "agent not found" 와 DB 오류를 구분해, 시스템 댓글/run_event/HTTP 응답이 운영 진단성에 도움이 되게 한다.

### 구현 태스크

- [x] `enqueueAutoChainMention`에서 `errors.Is(err, ErrNotFound)`와 그 외 store error를 분기. 호출자(`CompleteRunWithReason`)가 err 반환 시 트랜잭션을 rollback하므로 두 케이스 모두 (false, nil) swallow 흐름 유지(결과 댓글 보존).
- [x] not-found: 기존 메시지 `"자동 체이닝 대상 @X을 찾을 수 없습니다."` 유지.
- [x] store error: 별도 메시지 `"자동 체이닝 agent 조회 중 일시적 오류가 발생했습니다. 운영자에게 알려주세요."` — raw err 정보 노출 차단(SQL fragment/credential 누수 방지).
- [x] 메시지 분기 로직을 `autoChainAgentLookupMessage(mention, err) string` 헬퍼로 추출해 단위 검증 가능.
- [x] `internal/store/auto_chain_test.go`에 두 케이스 추가 — `TestAutoChainAgentLookupMessageDistinguishesNotFoundFromStoreError` (헬퍼 직접 검증: not-found vs wrapped not-found vs store error vs leak 없음) + `TestAutoChainMentionNotFoundProducesNotFoundComment` (통합: 존재하지 않는 agent 멘션 → not-found 메시지만 시스템 코멘트에 기록, transient 메시지는 없음).

### 자체 테스트

- [x] `go test -race ./internal/store/...` 통과

### 이슈 및 수정

- [x] store error 분기를 통합 테스트로 재현하기 어려움 — DB connection drop을 mock하지 않고 헬퍼 단위 테스트로 검증. 통합 회귀는 not-found 분기로 cover.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료

## Phase 5. `PUT /api/agents/:id` 계약 명시

### 목표

- 현재 핸들러가 `CreateAgentInput`을 통째로 받아 full-update로 동작하는 사실을 계약/문서/테스트가 명확히 한다. partial-update 도입은 호환성 영향이 있어 본 plan 범위 밖.

### 구현 태스크

- [x] `docs/API.md` §2.5 `PUT /api/agents/:id`를 full-replace로 명시 + 누락 시 빈 값으로 초기화되는 사이드이펙트와 GET→modify→PUT 권장 패턴, contract test 인용 추가.
- [x] `internal/httpapi/patches_test.go`에 `TestUpdateAgentIsFullReplacePinsContract` 추가 — `summary`/`tags`를 의도적으로 누락한 PUT 후 빈 문자열로 초기화됨을 명세.

### 자체 테스트

- [x] `go test -race ./internal/httpapi/...` 통과 (전체 회귀)
- [x] docs/API.md diff가 실제 핸들러 동작과 일치 (test가 docs를 enforce)

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료

## Phase 6. Vite/esbuild dev-only advisory 대응

### 목표

- `pnpm audit` dev 영역의 moderate 2건을 제거하거나, 제거 불가능한 경우 사유를 명시한다. prod audit은 clean 유지.

### 구현 태스크

- [x] `pnpm audit --prod` clean 재확인.
- [x] `pnpm audit` 전체 → 2 moderate (esbuild `<=0.24.2` via vite, vite `<=6.4.1` `.map` 경로 traversal) 확인.
- [x] vite를 `^6.4.2`로 명시(esbuild 0.25.x 동반 업데이트로 두 advisory 모두 해소). vite 8은 `@vitejs/plugin-react` peer 충돌로 보류, vite 6.4.x가 안전한 minimum.
- [x] vitest는 transitive vite 5.x를 들고 있어 그대로면 advisory 잔류 → `vitest@latest`로 같이 갱신.

### 자체 테스트

- [x] `pnpm --filter web build` 통과 (vite 6.4.x, 541 modules)
- [x] `pnpm --filter web test` 통과 (vitest 14/14)
- [x] `pnpm audit --prod` clean
- [x] `pnpm audit` 0 vulnerabilities

### 이슈 및 수정

- [x] vite 첫 시도는 `vite@latest` (8.0.14)였으나 `@vitejs/plugin-react@4.7.0`의 peer dependency가 `vite ^4-^7`만 지원 → vite ^6.4.2로 좁혀 해결.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료

## Phase 7. TODO / ROADMAP / CHANGELOG / API 문서 현재화

### 목표

- 문서가 commit `e619548` 기준 현실과 일치하게 갱신된다.

### 구현 태스크

- [x] `TODO.md` head에 "## Backlog — Phase 2+ (2026-05-21 갱신)" 섹션 추가 — 본 plan에서 처리된 P1·P2 항목 [x], 큰 기능·운영·잔여 후속은 미체크.
- [x] `CHANGELOG.md` `[Unreleased]` 섹션에 2026-05-20 / 2026-05-21 commit 묶음(`294fe2e` chain hub / `25c1ec2` UTF-8+noise / `e619548` run_event+위생 / `27021cc` execution SQL / `13bc9ed` Gemini stdin / `9770c05` auto_close UI / `e458c05` mention split) + migration 0017 추가.
- [x] `docs/API.md` §2.5 `PUT /api/agents/:id` 계약 명확화 (Phase 5에서 처리).
- [ ] `docs/ROADMAP.md` stale unchecked 항목 재검토 — 본 plan 범위 밖. backlog로 이관 또는 별도 commit으로 처리.

### 자체 테스트

- [x] 갱신된 markdown 파일 lint/preview 정상 (수동 cross-read)
- [x] CHANGELOG의 commit 해시가 실제 main 푸시본과 일치

### 이슈 및 수정

- [x] ROADMAP은 큰 갱신이 필요해 본 plan에서 보류. TODO.md backlog에서 추적.

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 묶음 commit + main push

## 잔여 리스크 / 후속 과제 (별도 plan)

- **per-run worktree** — workspace 동시 실행 활성화. git 상태 충돌/락 정책 필요.
- **workspace import/export** — workspace + agents + skills + autopilot + issue history 직렬화. 민감정보 마스킹 옵션.
- **외부 webhook** — issue/run 종료 이벤트 발신. retry / signing secret / delivery log.
- **attachment** — issue/comment file attach. 사이즈/보존/보안 정책.
- **Auto-chain 고급 UI** — chain tree/sequence 편집, stage resume, hub re-entry 정책 가시화.
- **Homebrew / LICENSE / demo seed / 대량 데이터 성능** — 운영/릴리스 트랙.
