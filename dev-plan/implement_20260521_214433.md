# implement_20260521_214433.md

작성 일시: `2026-05-21 21:44:33 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

직전 plan(`implement_20260521_211716.md`)의 후속으로, 두 트랙을 함께 진행한다.

| 트랙 | 카테고리 | 핵심 |
|---|---|---|
| **A** | B2 demo seed | `cron-agent-dashboard seed` CLI — fresh clone에서 workspace + main agent(hub-PM instructions) + worker agents + auto_chain_enabled가 한 번에 박혀 README quick start가 e2e로 동작하게 한다 |
| **B** | Auto-chain 고급 UI (Phase 2+ backlog 첫 항목) | IssueFlowGraph에 hub-PM 재진입 / chain_depth / stage 가시화 — 직전 두 plan의 정책 변경(hub re-entry exemption, depth policy)을 사용자가 직접 그래프로 확인할 수 있게 |

backlog 큰 기능의 나머지 4개(per-run worktree / import-export / webhook / attachment)는 본 plan 범위 밖. 각각 별도 plan 후속.

## 개발 범위

### Track A — seed CLI

- `internal/app/seed.go` 신설 — `SeedExample(ctx, *store.Store) (Workspace, error)`. workspace + main agent(Lead, codex, hub-PM instructions) + worker agents(Writer, Reviewer 등) + auto_chain_enabled=true + 데모 이슈 1건.
- `cmd/cron-agent-dashboard/main.go`에 `seed` 명령 추가. 인자가 없으면 default example. 이미 같은 slug가 있으면 idempotent하게 skip + 경고.
- `internal/app/seed_test.go` — 두 번 호출해도 race 없이 idempotent하고, 생성된 자원이 README quick start 흐름에 부합함을 검증.
- README 빠른 시작 섹션에 `cron-agent-dashboard seed` 한 단계 추가.

### Track B — Auto-chain 그래프 가시화

- `web/src/components/IssueFlowGraph.tsx`의 노드/엣지에 hub-PM 재진입을 시각적으로 구분(예: main agent 노드 색상 + chain_depth 라벨).
- 같은 chain의 worker 시퀀스가 max_depth 한계에 가까워질 때 헤더에 indicator 표시(예: `chain_depth N / max M`).
- vitest로 IssueFlowGraph 변경 회귀 1 케이스 추가(가능한 범위 내).

## 제외 범위

- per-run worktree, import/export, webhook, attachment — backlog 큰 기능 4개. 본 plan 범위 밖.
- chain tree 편집 / stage resume 같은 운영 액션 — Track B는 가시화만, 액션은 후속.
- seed 데이터를 사용자 정의(`seed <profile>`)로 다양화 — 본 plan은 단일 example만.
- README/docs를 README quick start 외 영역까지 큰 폭으로 갱신 — 본 plan 범위 밖.

## 참조 문서

- [README](../README.md)
- [docs/CHAINING](../docs/CHAINING.md)
- [internal/store/auto_chain.go](../internal/store/auto_chain.go)
- [internal/store/workspace.go](../internal/store/workspace.go) — `CreateWorkspaceWithMainAgent`
- [internal/store/skills.go](../internal/store/skills.go) — `UpsertSkill`
- [web/src/components/IssueFlowGraph.tsx](../web/src/components/IssueFlowGraph.tsx)
- 이전 plans: [implement_20260521_211716.md](./implement_20260521_211716.md), [implement_20260521_191038.md](./implement_20260521_191038.md), [implement_20260520_230031.md](./implement_20260520_230031.md)

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- TDD-first: 변경 동작은 먼저 명세 테스트(failing) → 패치 → 통과.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- Track A·B는 각각 commit/push로 분리. Track A가 먼저 (storage가 안정해야 UI 가시화도 신뢰 가능).

## Phase 상태 요약

- [x] Phase A1 — `internal/app/seed.go` + `SeedExample` + 테스트
- [x] Phase A2 — `cron-agent-dashboard seed` 명령 + README quick start 한 줄
- [ ] Phase B1 — IssueFlowGraph 노드/엣지 hub-PM 재진입 가시화 + chain_depth 라벨

## Phase A1. `SeedExample` 함수 + 테스트

### 목표

- store API만으로 example workspace를 시드할 수 있는 단일 함수를 만든다. CLI 진입점과 분리된 형태로 테스트 가능.

### 구현 태스크

- [x] `internal/app/seed.go` 신설 — `SeedExample(ctx, *store.Store) (SeededExample, error)`. workspace `데모 협업 스튜디오` (slug `demo-studio`, prefix `DEMO`) + `Lead`(codex, hub-PM 가드 instructions) + `Writer` + `Reviewer`. workspace는 `auto_chain_enabled=true`.
- [x] Idempotency: `store.GetWorkspace(slug)`이 성공하면 기존 workspace + main agent + worker 목록을 그대로 돌려주고 `AlreadyHad=true`로 표시. ErrNotFound가 아닌 다른 에러는 그대로 반환.
- [x] `internal/app/seed_test.go` — 첫 호출 후 자원 검증(hub guard 텍스트 포함 / Writer·Reviewer 존재) + idempotency 검증(`AlreadyHad=true` / 자원 수 동일).

### 자체 테스트

- [x] `go test -race ./internal/app/...` 통과
- [x] `go test -race ./...` 클린 (10 패키지)

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase A2. `seed` CLI 명령 + README quick start

### 목표

- `./cron-agent-dashboard seed`가 fresh clone에서 즉시 동작하는 데모 환경을 만든다. README quick start에 한 줄로 안내.

### 구현 태스크

- [x] `cmd/cron-agent-dashboard/main.go`에 `case "seed":` 분기 + `seedExample(st)` helper 추가. 결과(첫 호출/idempotent)에 맞춰 stdout에 친절한 메시지.
- [x] README 빠른 시작 "초기화 + 실행" 블록에 한 줄 추가(선택적임을 주석으로 명시).
- [x] dev 환경 smoke — `./cron-agent-dashboard seed --data-dir <tmp>` 첫 호출/두 번째 호출 모두 검증.

### 자체 테스트

- [x] `go build ./...` 통과
- [x] dev smoke 1회 + idempotent 2회 통과 (unit test가 일부 cover)

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] commit + main push

## Phase B1. IssueFlowGraph 가시화 보강

### 목표

- 사용자가 issue detail에서 chain 흐름을 볼 때 hub-PM 재진입과 chain_depth를 직접 인지할 수 있게 한다.

### 구현 태스크

- [ ] `web/src/components/IssueFlowGraph.tsx` 노드 라벨에 `chain_depth=N` 표시. main agent 재진입 노드는 시각적 강조(border 또는 color).
- [ ] 그래프 헤더 또는 legend에 짧은 문구 — "main agent 재진입은 depth 카운트에서 제외됩니다" (B4 정책 가시화).
- [ ] 기존 vitest IssueFlowGraph 테스트가 깨지지 않게 유지. 회귀가 필요하면 1 케이스 추가.

### 자체 테스트

- [ ] `pnpm --filter web build` 통과
- [ ] `pnpm --filter web test` 통과

### 이슈 및 수정

- [ ] 발견 이슈 없음

### 완료 조건

- [ ] 구현 완료
- [ ] 자체 테스트 완료
- [ ] commit + main push

## 잔여 리스크 / 후속 과제 (이번 plan 범위 밖)

- **Per-run worktree** — workspace 동시 실행 활성화. git 상태 충돌 정책.
- **Workspace import/export** — workspace + agents + skills + autopilot + issue history 직렬화 + 민감정보 마스킹.
- **외부 webhook** — issue/run 종료 이벤트 발신. retry / signing secret / delivery log.
- **첨부 파일** — issue/comment file attachment. 사이즈/보존/보안.
- **Auto-chain 고급 UI 후속 단계** — chain tree 편집, stage-level resume/cancel/retry 운영 액션.
- **C2 claude `--print` 정공 fix** — Lead runtime을 codex로 우회 중. 별도 plan.
