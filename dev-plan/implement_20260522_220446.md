# implement_20260522_220446.md

작성 일시: `2026-05-22 22:04:46 KST`

본 plan은 Backlog Phase 2+ 의 모든 잔여 후속 항목을 닫는 **최종 마무리 사이클**이다.

## 개발 목적

`TODO.md` 의 유일하게 남은 미체크 항목(대량 데이터 성능 검증) + 직전 사이클들에서 "별도 plan으로 명시 이관" 한 7항목까지 한 사이클로 마무리한다. 큰 명세 변경 없이 한 commit으로 닫을 수 있는 항목들로 좁힌다. 외부 storage(S3/MinIO)와 바이러스 스캔은 single-binary 로컬 distribution thesis와 충돌하므로 superseded로 마감한다.

## 개발 범위

- **Track A — 대량 데이터 성능 benchmark**: 1,000개 issue + 5,000 comment + 10,000 run 시드 후 ListIssues / ListRuns / ListComments 응답시간 측정. 발견된 hot path는 인덱스 추가.
- **Track B — chain depth/cost guard 시각화**: ChainSummaryPanel에 workspace.auto_chain_max_depth / auto_chain_daily_cost_micros 대비 현재 chain 사용량 표시.
- **Track C — chain retry**: `POST /api/runs/chain/{chain}/retry` — 마지막 failed step부터 같은 agent로 새 run 생성.
- **Track D — webhook 다회 retry + exponential backoff + dead-letter**: 현재 1회 retry를 5회 지수 backoff로 확장. `webhook_delivery.status='failed'` 가 됐을 때 dead-letter 표시.
- **Track E — attachment image inline preview**: IssueAttachmentsPanel이 content_type 시작이 `image/`이면 download URL을 `<img>`로 렌더.
- **Track F — attachment comment 첨부 연결**: 댓글 폼에서 첨부 업로드 → 첨부의 `comment_id` 필드 연결 (스키마는 attachment.comment_id nullable로 확장).
- **Track G — workspace 차원 chain dashboard**: 새 페이지 `/w/:slug/chains` — workspace 전체 chain summary list.
- **Track H — superseded 명시 마감**: S3 외부 storage / ClamAV 바이러스 스캔은 single-binary 토픽과 충돌하므로 TODO/ROADMAP에 superseded 표기.

## 제외 범위

- 다국어(i18n) — 영원히 deferred.
- attachment 가상 storage abstraction(plug-in interface) — superseded와 함께 닫음.
- workspace dashboard의 그래프/차트 시각화 — 텍스트 카드 list만.

## 참조 문서

- [TODO.md](../TODO.md) — Backlog Phase 2+ 잔여.
- 직전 사이클: [implement_20260522_212332.md](./implement_20260522_212332.md), [implement_20260522_183344.md](./implement_20260522_183344.md).
- [internal/store/cancellation.go](../internal/store/cancellation.go) — chain cancel 베이스(Track D 참조).
- [internal/app/webhook_dispatcher.go](../internal/app/webhook_dispatcher.go) — webhook dispatcher 기존 retry.

## 공통 진행 규칙

- TDD-first.
- 트랙별 1 commit. trailer `Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>`.
- Phase 완료 시 TODO/플랜 갱신 후 main push.
- 본 plan에 없는 범위 확장 금지.

## Phase 상태 요약

- [x] Track A. 대량 데이터 성능 benchmark + 인덱스(불필요)
- [x] Track B. chain depth/cost guard 시각화
- [x] Track C. chain retry
- [x] Track D. webhook 다회 retry + exponential backoff + dead-letter
- [x] Track E. attachment image inline preview
- [x] Track F. attachment comment 첨부 연결
- [x] Track G. workspace chain dashboard
- [x] Track H. S3/바이러스 스캔 superseded 마감

---

## Track A. 대량 데이터 성능 benchmark + 인덱스

### 목표

이슈 1,000개, comment 5,000개, run 10,000개 시드 후:
- `ListIssues(workspace)` < 200ms
- `ListRuns(issueID)` < 50ms
- `ListComments(issueID)` < 50ms

### 구현 태스크

- [x] Phase A.1 — `perf_bench_test.go` — 1,000 issue + 5,000 comment + 10,000 run 시드 helper + 3개 Benchmark + 1 assertion test(`TestLargeDatasetMeetsLatencyBudgets`).
- [x] Phase A.2 — 측정 결과 (Apple M4, SQLite local): ListIssues@1k issues 1.2ms / ListRuns 137µs / ListComments 67µs. 모두 budget의 1/100 미만이라 인덱스 추가 불필요. 향후 회귀 가드는 assertion test가 담당.

### 자체 테스트

- [x] `go test -bench BenchmarkLargeDataset -run=^$ ./internal/store/...` 통과
- [x] `go test -race ./...` 통과

---

## Track B. chain depth/cost guard 시각화

### 목표

`ChainSummaryPanel`의 각 chain row에 max_depth / daily_cost limit 대비 사용량을 표시.

### 구현 태스크

- [x] Phase B.1 — `ChainSummaryPanel`에 `workspace` prop + `classifyGuard()` 헬퍼. 75% 이상이면 `warn`, 100% 넘으면 `over`.
- [x] Phase B.2 — IssueDetailPage에서 `useWorkspaceQuery(slug)`로 workspace 전달.
- [x] Phase B.3 — vitest 2건 (depth 근접 warn / cost 초과 over) + 기존 4건 회귀. global.css에 `data-guard` 토큰 색 추가.

### 자체 테스트

- [x] `pnpm --filter web test` 통과
- [x] `pnpm --filter web build` 통과

---

## Track C. chain retry

### 목표

`POST /api/runs/chain/{chain}/retry` — 같은 chain에서 가장 최근 failed run의 agent로 새 run 생성. chain_id 이어가기.

### 구현 태스크

- [x] Phase C.1 — `TestRetryFailedRunInChain*` 3건 — 정상/queued면 거절/failed 없으면 NotFound.
- [x] Phase C.2 — `Store.RetryFailedRunInChain(ctx, chainID) (Run, error)`. ChainID/Depth 보존, parent_run_id 연결, trigger_type='rerun'.
- [x] Phase C.3 — `POST /api/runs/chain/{chain}/retry` + ChainSummaryPanel "체인 재시작" 버튼 (마지막 status가 failed일 때만).

### 자체 테스트

- [x] `go test -race ./internal/store/...` 통과
- [x] `go test -race ./internal/httpapi/...` 통과
- [x] `pnpm --filter web test` 통과

---

## Track D. webhook 다회 retry + exponential backoff + dead-letter

### 목표

현재 1회 retry → 5회로 확장. 실패 시 지수 backoff (30s, 2m, 8m, 30m, 2h). 최종 5회 실패 시 status='failed'로 박고 Settings UI에 dead-letter 카운트 표시.

### 구현 태스크

- [x] Phase D.1 — `TestWebhookDispatcherExpBackoffEventuallyDeadLetters` (httptest 5xx + 6회 tick → status='failed') + `TestWebhookDispatcherBackoffSchedule` (per-failure index + clamp 검증).
- [x] Phase D.2 — `WebhookDispatcher.retryBackoffs []time.Duration` + `DefaultWebhookRetryBackoffs = [30s, 2m, 8m, 30m, 2h]` + `backoffForAttempt` clamp. 기본 `maxAttempts=6` (initial + 5 retries).
- [x] Phase D.3 — `Store.CountWebhookDeliveryFailed` + `webhookView.failed_delivery_count` + WorkspaceWebhookSection "dead-letter N건" 배지.

### 자체 테스트

- [x] `go test -race ./internal/store/...` 통과
- [x] `go test -race ./internal/app/...` 통과
- [x] `pnpm --filter web test` 통과
- [x] `pnpm --filter web build` 통과

---

## Track E. attachment image inline preview

### 목표

`IssueAttachmentsPanel` 에서 content_type 이 `image/`로 시작하면 `<img>` thumb를 inline 렌더.

### 구현 태스크

- [ ] Phase E.1 — `web/src/components/IssueAttachmentsPanel.tsx` 분기 추가.
- [ ] Phase E.2 — vitest.

### 자체 테스트

- [ ] `pnpm --filter web test` 통과

---

## Track F. attachment comment 첨부 연결

### 목표

attachment 테이블에 nullable `comment_id` 컬럼 추가. CommentForm에서 첨부 시 comment_id에 연결.

### 구현 태스크

- [ ] Phase F.1 — migration 0024 — `attachment.comment_id TEXT NULL REFERENCES comment(id) ON DELETE SET NULL`.
- [ ] Phase F.2 — Comment 작성 시 attachment.comment_id 갱신 (`UPDATE attachment SET comment_id=?`).
- [ ] Phase F.3 — UI: 댓글 영역에서 첨부 가능, 댓글에 첨부 칩 표시.

### 자체 테스트

- [ ] `go test -race ./...` 통과
- [ ] `pnpm --filter web test` 통과

---

## Track G. workspace 차원 chain dashboard

### 목표

새 페이지 `/w/:slug/chains` — workspace 전체 chain summary list. 사이드바에 메뉴 추가.

### 구현 태스크

- [ ] Phase G.1 — store: `ListChainsByWorkspace(ctx, workspaceID, limit) ([]ChainSummary, error)`.
- [ ] Phase G.2 — `GET /api/workspaces/{slug}/chains`.
- [ ] Phase G.3 — 새 페이지 + 사이드바.

### 자체 테스트

- [ ] `go test -race ./...` 통과
- [ ] `pnpm --filter web build` 통과

---

## Track H. S3 외부 storage / 바이러스 스캔 superseded 마감

### 목표

TODO.md, ROADMAP.md에 명시. 코드 변경 없음.

### 구현 태스크

- [x] Phase H.1 — TODO.md에 "Superseded — single-binary local thesis와 충돌하므로 close" 절 신설. S3/MinIO storage, ClamAV 바이러스 스캔, 워커 메모리 측정 항목을 close 라벨로 마감.

---

## 잔여 리스크 / 후속 과제

본 plan으로 모든 backlog 항목이 닫힌다. 향후 후속은 운영 중 발견된 새 항목으로만 시작한다.
