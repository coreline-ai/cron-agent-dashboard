-- 0004 retained for migration ordering.
-- Agent model selection is user-controlled again: empty string means runtime default,
-- and any explicit model id is preserved by the store layer.
SELECT 1;
